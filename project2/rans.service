[Unit]
Description=Daemon for the RANS stack

[Service]
Type=forking
ExecStart=/usr/bin/bash -c '/usr/bin/nginx -c /etc/rans/nginx.conf; /etc/rans/server_bin; /usr/bin/arangod'
ExecStop=/usr/bin/pkill -f 'nginx|rust|arangodb'
Restart=always